/* 
    * PURE FUNCTION
        - it returns the same arguments
        - it doesn't affected by any external input change
*/

function calculateGST(productPrice: number) {
  return productPrice * 0.05;
}
console.log(calculateGST(15));


/* 
    * 
*/

